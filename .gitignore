# ================================================================
# data/ —— 白名单模式：默认全忽略，只放行定池元数据 JSON（跑回测需要）
# 数据库 / 模型缓存 / 回测输出 / 运行状态 / 快照一律不进版本库
# 注意：gitignore 不支持行尾注释，放行规则必须独占一行
# ================================================================
data/*
# 定池元数据白名单（.gitignore 规则：注释必须独立成行，不能跟在规则后面）
!data/tradfi_symbols.json
!data/no_spot_symbols.json
!data/spot_onboard_dates.json
!data/spot_base.json
!data/onboard_dates.json
!data/universe_long.json
*.db
*.db-wal
*.db-shm

# 本地密钥（真实 key 只留在这台机器，不提交）
monitor/credentials.py

# 编辑器 / 本地配置 / 缓存
.idea/
.claude/
.vscode/
__pycache__/
*.pyc
*.pyo

# 运行产生的 CSV 快照
*.csv

# WorkBuddy 工作记忆（内部认知、实盘持仓、策略思路——不公开）
.workbuddy/

# 部署环境变量（含密钥/服务器信息，不公开）
*.env
*.env.*
live.env

# 运行输出
outputs/